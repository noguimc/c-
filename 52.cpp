#include <bits/stdc++.h>
using namespace std;
struct Node{
    int data;
    Node *next;
}

int main(){
    Node *head=new Node;
    head->data=0;
    head->next=NULL;
    Node *p
    int n=4;
    for(int i=1;i<=n;i++){
        Node *p=new Node;
        cin>>q->data;
        q->next=NULL;
        p->next=q;
        p=q;
    }
}